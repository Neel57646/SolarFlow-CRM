[project]
name = "solarflow-crm"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "django==5.2.1",
  "psycopg[binary]>=3.2,<4",
  "whitenoise>=6.8,<7",
]

[project.scripts]
app = "solarflow.wsgi:app"
